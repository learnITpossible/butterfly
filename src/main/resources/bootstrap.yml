## config server
#spring:
#  cloud:
#    config:
#      uri: http://localhost:8888

## eureka
spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: zoo-island  # config server's application name
eureka:
  client:
    serviceUrl: